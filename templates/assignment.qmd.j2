---
title: "{{ module.title }}: {{ i18n.assignment.title_suffix }}"
{% if module.date is defined %}date: "{{ module.date }}"
{% endif %}author: "{{ course.instructors | map(attribute='name') | join(', ') }}"
organization: "{{ course.organization.institution }}"
institute: "{{ course.organization.institute }}"
email: "{{ course.instructors | selectattr('email', 'defined') | map(attribute='email') | join(', ') }}"
course: "{{ course.title }}"
term: "{{ course.semester }}"
lang: "{{ course.language | default('en') }}"
format:
  html: default
  pdf:
    documentclass: article
    papersize: a4
    include-in-header: ../../assets/latex/_handout-packages.tex
    template-partials:
      - ../../assets/latex/before-body.tex
{% if module.lms is defined and module.lms.due %}due: "{{ module.lms.due }}"
{% endif %}reference-section-title: "{{ i18n.assignment.references }}"
---

## {{ i18n.assignment.instructions }}

TODO: Describe the assignment task.

## {{ i18n.assignment.tasks }}

1. TODO

## {{ i18n.assignment.submission }}

TODO: Add submission instructions.
